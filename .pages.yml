backend:
  name: git-gateway
  branch: main
collections:
  - name: pages
    label: Pages
    folder: .
    format: html
    create: true
    fields:
      - { name: title, label: Title, widget: string }
      - { name: body, label: Body, widget: markdown }